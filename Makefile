up: ## docker compose up
	docker compose up
up-d: ## docker compose up -d
	docker compose up -d
up-with-build: ## docker-compose up -d
	docker compose up --build
down: ## docker compose down
	docker compose down
